os:
  - linux
  - osx

language: node_js

node_js:
  - 8

jobs:
  include:
    - stage: npm release
      os: linux
      deploy:
        provider: npm
        api_key: $NPM_API_KEY
        on:
          tags: true
          branch: master
          repo: anishkny/webgif
